#include <iostream> 
#include <fstream>
#include <windows.h>
#include <iomanip>
using namespace std;

int main()
{
SetConsoleOutputCP(1251);
SetConsoleCP(1251);
const int MAX = 400;
char str[MAX];
cout << "Введите стихотворение (
